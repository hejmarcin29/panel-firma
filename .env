# Base environment defaults shared in the repository.
# Copy this file to .env.local and override values per environment.

DATABASE_URL=file:sqlite.db

# Secrets and environment-specific settings can be overridden in .env.local.
# Panel Ustawienia zapisuje sekret webhooka w bazie, wpis ponizej traktuj jako opcjonalny fallback.
WOOCOMMERCE_WEBHOOK_SECRET=

# wFirma integration (formularz w panelu ustawia wartosci w bazie; wpisz tutaj ewentualne wartosci domyslne)
WFIRMA_LOGIN=
WFIRMA_API_KEY=
WFIRMA_TENANT=
