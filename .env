# Base environment defaults shared in the repository.
# Copy this file to .env.local and override values per environment.

DATABASE_URL=file:sqlite.db

# Secrets and environment-specific settings should live in .env.local.
WOOCOMMERCE_WEBHOOK_SECRET=

# wFirma integration (fill values in .env.local)
WFIRMA_CLIENT_ID=
WFIRMA_CLIENT_SECRET=
WFIRMA_TENANT=
WFIRMA_REDIRECT_URI=http://localhost:3000/api/wfirma/callback
