services:
  app:
    build:
      context: .
    environment:
      NODE_ENV: "production"
      PORT: "3000"
      DATABASE_URL: "/app/data/panel.db"
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
