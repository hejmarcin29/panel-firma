{
  admin off
  # Optional: ACME email for notifications
  # email admin@your-domain
}

# Replace YOUR_DOMAIN with your real domain
YOUR_DOMAIN {
  encode zstd gzip
  reverse_proxy app:3000

  header {
    Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
  }
}
